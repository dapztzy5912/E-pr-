<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Terjadi Kesalahan | E-KTP Generator Indonesia</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="https://raw.githubusercontent.com/YoshCasaster/store_db/refs/heads/main/Template.png">
    <link rel="apple-touch-icon" href="https://raw.githubusercontent.com/YoshCasaster/store_db/refs/heads/main/Template.png">
    
    <!-- Meta Tags -->
    <meta name="description" content="Terjadi kesalahan pada E-KTP Generator Indonesia. Silakan coba lagi.">
    <meta name="robots" content="noindex, nofollow">
    
    <!-- Styles -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- Modern Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <i class="bi bi-person-vcard me-2"></i>
                <span>E-KTP Generator</span>
            </a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="bi bi-house-door me-1"></i>Beranda
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="https://github.com/YoshCasaster" target="_blank" rel="noopener">
                            <i class="bi bi-github me-1"></i>GitHub
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Error Hero Section -->
    <div class="container mt-5">
        <div class="row justify-content-center min-vh-75 align-items-center">
            <div class="col-12 col-md-8 col-lg-6">
                <!-- Error Animation -->
                <div class="text-center mb-4">
                    <div class="error-animation mb-4">
                        <div class="error-icon">
                            <div class="error-circle">
                                <div class="error-line error-line-1"></div>
                                <div class="error-line error-line-2"></div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Error Card -->
                <div class="card shadow-lg border-0 error-card">
                    <div class="card-header text-center border-0">
                        <h1 class="text-white mb-0 display-6 fw-bold">
                            <i class="bi bi-exclamation-triangle-fill me-2"></i>
                            Oops! Terjadi Kesalahan
                        </h1>
                        <p class="text-white-50 mb-0 mt-2">
                            Sepertinya ada yang tidak beres dengan permintaan Anda
                        </p>
                    </div>
                    <div class="card-body p-4 text-center">
                        <!-- Error Message -->
                        <div class="mb-4">
                            <div class="alert alert-danger border-0 shadow-sm">
                                <div class="d-flex align-items-center justify-content-center">
                                    <i class="bi bi-bug-fill me-2 fs-4"></i>
                                    <div>
                                        <h5 class="alert-heading mb-1">Pesan Error:</h5>
                                        <p class="mb-0 fw-semibold"><%= message %></p>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Suggestions -->
                        <div class="mb-4">
                            <h5 class="fw-bold mb-3">
                                <i class="bi bi-lightbulb me-2 text-warning"></i>
                                Saran Penyelesaian:
                            </h5>
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card border-0 bg-light">
                                        <div class="card-body p-3">
                                            <ul class="list-unstyled mb-0">
                                                <li class="mb-2">
                                                    <i class="bi bi-check-circle text-success me-2"></i>
                                                    Pastikan semua field telah diisi dengan benar
                                                </li>
                                                <li class="mb-2">
                                                    <i class="bi bi-check-circle text-success me-2"></i>
                                                    Periksa format file foto (JPG/PNG, maksimal 5MB)
                                                </li>
                                                <li class="mb-2">
                                                    <i class="bi bi-check-circle text-success me-2"></i>
                                                    Pastikan koneksi internet Anda stabil
                                                </li>
                                                <li class="mb-0">
                                                    <i class="bi bi-check-circle text-success me-2"></i>
                                                    Coba refresh halaman dan ulangi proses
                                                </li>
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="d-flex flex-column flex-md-row gap-3 justify-content-center">
                            <a href="/" class="btn btn-primary btn-lg">
                                <i class="bi bi-house-door me-2"></i>
                                Kembali ke Beranda
                            </a>
                            <button type="button" class="btn btn-outline-primary btn-lg" onclick="history.back()">
                                <i class="bi bi-arrow-left me-2"></i>
                                Kembali ke Halaman Sebelumnya
                            </button>
                            <button type="button" class="btn btn-outline-secondary btn-lg" onclick="location.reload()">
                                <i class="bi bi-arrow-clockwise me-2"></i>
                                Refresh Halaman
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Help Section -->
                <div class="card border-0 shadow-sm mt-4">
                    <div class="card-body p-4">
                        <div class="text-center">
                            <h5 class="fw-bold mb-3">
                                <i class="bi bi-question-circle me-2 text-info"></i>
                                Masih Mengalami Masalah?
                            </h5>
                            <p class="text-muted mb-3">
                                Jika error ini terus terjadi, kemungkinan ada masalah teknis sementara. 
                                Silakan coba beberapa saat lagi atau hubungi developer.
                            </p>
                            <div class="d-flex flex-column flex-sm-row gap-2 justify-content-center">
                                <a href="https://github.com/YoshCasaster" target="_blank" rel="noopener" class="btn btn-outline-dark">
                                    <i class="bi bi-github me-2"></i>
                                    Hubungi Developer
                                </a>
                                <button type="button" class="btn btn-outline-info" id="reportError">
                                    <i class="bi bi-flag me-2"></i>
                                    Laporkan Error
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Error Code (if available) -->
                <div class="text-center mt-3">
                    <small class="text-muted">
                        Error Code: ERR_<%= Date.now().toString().slice(-6) %> | 
                        Time: <%= new Date().toLocaleString('id-ID') %>
                    </small>
                </div>
            </div>
        </div>
    </div>

    <!-- Modern Footer -->
    <footer class="mt-5">
        <div class="container">
            <div class="text-center">
                <div class="mb-3">
                    <h5 class="text-white fw-bold">E-KTP Generator Indonesia</h5>
                    <p class="text-white-50 mb-0">Tool modern untuk membuat simulasi E-KTP</p>
                </div>
                <div class="border-top border-secondary pt-3">
                    <p class="mb-0 text-white-50">
                        Dibuat dengan <i class="bi bi-heart-fill text-danger"></i> oleh 
                        <a href="https://github.com/YoshCasaster" target="_blank" rel="noopener" class="text-decoration-none fw-bold">ZenmaTH</a>
                    </p>
                    <small class="text-white-50 d-block mt-1">
                        © 2024 - Hanya untuk keperluan edukasi
                    </small>
                </div>
            </div>
        </div>
    </footer>

    <!-- Toast Container -->
    <div class="toast-container position-fixed top-0 end-0 p-3"></div>
    
    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Custom Scripts -->
    <script>
        // Error animation trigger
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(() => {
                document.querySelector('.error-animation').classList.add('animate');
            }, 300);
        });

        // Report error functionality
        document.getElementById('reportError')?.addEventListener('click', function() {
            const errorDetails = {
                message: '<%= message %>',
                timestamp: new Date().toISOString(),
                userAgent: navigator.userAgent,
                url: window.location.href
            };

            // Create mailto link with error details
            const subject = encodeURIComponent('Error Report - E-KTP Generator');
            const body = encodeURIComponent(`Error Details:\n\nMessage: ${errorDetails.message}\nTimestamp: ${errorDetails.timestamp}\nBrowser: ${errorDetails.userAgent}\nURL: ${errorDetails.url}\n\nPlease describe what you were doing when this error occurred:\n\n`);
            
            window.location.href = `mailto:?subject=${subject}&body=${body}`;
            
            showToast('Email client terbuka. Silakan kirim laporan error.', 'info');
        });

        // Toast notification function
        function showToast(message, type = 'info') {
            const toastContainer = document.querySelector('.toast-container');
            const toastId = 'toast-' + Date.now();
            
            const toastHtml = `
                <div class="toast" id="${toastId}" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="toast-header bg-${type === 'success' ? 'success' : type === 'error' ? 'danger' : 'primary'} text-white">
                        <i class="bi bi-${type === 'success' ? 'check-circle' : type === 'error' ? 'exclamation-triangle' : 'info-circle'} me-2"></i>
                        <strong class="me-auto">Notifikasi</strong>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast"></button>
                    </div>
                    <div class="toast-body">
                        ${message}
                    </div>
                </div>
            `;
            
            toastContainer.insertAdjacentHTML('beforeend', toastHtml);
            const toastElement = document.getElementById(toastId);
            const toast = new bootstrap.Toast(toastElement, { delay: 4000 });
            toast.show();
            
            // Remove toast element after it's hidden
            toastElement.addEventListener('hidden.bs.toast', function() {
                this.remove();
            });
        }

        // Auto-retry functionality (optional)
        let retryCount = 0;
        const maxRetries = 3;
        
        function autoRetry() {
            if (retryCount < maxRetries) {
                retryCount++;
                showToast(`Mencoba ulang otomatis... (${retryCount}/${maxRetries})`, 'info');
                
                setTimeout(() => {
                    location.reload();
                }, 2000);
            }
        }

        // Uncomment if you want auto-retry functionality
        // setTimeout(autoRetry, 5000);
    </script>

    <!-- Additional CSS for animations -->
    <style>
        .min-vh-75 {
            min-height: 75vh;
        }

        .error-card {
            animation: fadeInUp 0.6s ease-out;
        }

        .error-card .card-header {
            background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
        }

        .error-animation {
            display: inline-block;
        }

        .error-icon {
            width: 80px;
            height: 80px;
            position: relative;
            margin: 0 auto;
        }

        .error-circle {
            width: 80px;
            height: 80px;
            border: 4px solid #dc3545;
            border-radius: 50%;
            position: relative;
            background: rgba(220, 53, 69, 0.1);
            animation: pulse 2s infinite;
        }

        .error-line {
            position: absolute;
            height: 4px;
            width: 40px;
            background-color: #dc3545;
            border-radius: 2px;
            top: 50%;
            left: 50%;
            transform-origin: center;
        }

        .error-line-1 {
            transform: translate(-50%, -50%) rotate(45deg);
            animation: drawLine 0.5s ease-out 0.3s both;
        }

        .error-line-2 {
            transform: translate(-50%, -50%) rotate(-45deg);
            animation: drawLine 0.5s ease-out 0.5s both;
        }

        @keyframes pulse {
            0% {
                box-shadow: 0 0 0 0 rgba(220, 53, 69, 0.4);
            }
            70% {
                box-shadow: 0 0 0 20px rgba(220, 53, 69, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(220, 53, 69, 0);
            }
        }

        @keyframes drawLine {
            0% {
                width: 0;
                opacity: 0;
            }
            100% {
                width: 40px;
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .error-animation.animate .error-circle {
            animation: pulse 2s infinite;
        }

        .error-animation.animate .error-line-1 {
            animation: drawLine 0.5s ease-out 0.3s both;
        }

        .error-animation.animate .error-line-2 {
            animation: drawLine 0.5s ease-out 0.5s both;
        }

        .card:hover {
            transform: translateY(-2px);
            transition: all 0.3s ease;
        }

        .btn {
            transition: all 0.3s ease;
        }

        .btn:hover {
            transform: translateY(-2px);
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .display-6 {
                font-size: 1.5rem;
            }
            
            .error-icon {
                width: 60px;
                height: 60px;
            }
            
            .error-circle {
                width: 60px;
                height: 60px;
            }
            
            .error-line {
                width: 30px;
            }
        }

        /* Dark mode support */
        @media (prefers-color-scheme: dark) {
            .bg-light {
                background-color: #2d3748 !important;
                color: #f7fafc;
            }
        }
    </style>
</body>
</html>
